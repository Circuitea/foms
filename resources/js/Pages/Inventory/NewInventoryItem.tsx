import Authenticated from "@/Layouts/AuthenticatedLayout";

export default function NewInventoryItem() {

  return (
    
  );
}

NewInventoryItem.layout = (e: JSX.Element) => <Authenticated children={e} />