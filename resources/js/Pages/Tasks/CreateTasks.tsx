import Authenticated from "@/Layouts/AuthenticatedLayout";

export default function CreateTasks() {
    return (
        <div>
            1
        </div>
    );
}

CreateTasks.layout = (e: JSX.Element) => <Authenticated children={e} />